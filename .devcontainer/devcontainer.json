{
  "name": "Official Node Container",
  "build": {
    "dockerfile": "Dockerfile"
  },
  //"workspaceFolder": "/app",
  "customizations": {
    "vscode": {
      "extensions": [
        // Optional: Add recommended VS Code extensions here (example: a spell checker)
        //"streetsidesoftware.code-spell-checker"
      ]
    }
  },
  // Optional: Post-create command to run after the container is created
  "postCreateCommand": "echo 'Development container ready!'"
}
